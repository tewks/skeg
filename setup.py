#!/usr/bin/env python

from distutils.core import setup

setup(name='skeg',
      version='1.0',
      description='easy fast persistent data models',
      author='tg',
      packages=['skeg'],
      install_requires=['diesel']
     )
